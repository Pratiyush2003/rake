import { FC } from 'react';

export const BasicModalContent: FC = () => {
  return (
    <section className='h-40 w-80 overflow-auto rounded bg-rake_grey-400 text-rake_blue-500'>
      Some content
    </section>
  );
};
