export enum EActionsTypes {
  DISCONNECT = 'DISCONNECT',
  CONNECT = 'CONNECT',
}

export enum ESocketMessages {
  CONNECTED = 'CONNECTED',
  DISCONNECT = 'DISCONNECT',
  DISCONNECTED = 'DISCONNECTED',
}
